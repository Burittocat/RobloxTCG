# scripts/bootstrap.cmd 가 받아 놓는 rojo·lune 실행 파일.
# 버전은 aftman.toml 이 들고 있으니 언제든 다시 받으면 된다.
.tools/

# Rojo 빌드 산출물 (소스에서 언제든 다시 만들 수 있다)
build.rbxlx
*.rbxl
*.rbxlx.lock
*.rbxm

# luau-lsp 가 자동 생성한다. 커밋하면 매번 충돌만 난다.
sourcemap.json

# 에디터
#   .vscode/ 는 일부러 커밋한다 — 클론하고 바로 같은 환경이 되도록.
.DS_Store

# Claude Code 개인 설정 (프로젝트 공용은 .claude/settings.json)
.claude/settings.local.json
